name: steady
description: "A Result type library for Dart inspired by Rust's Result<T, E>. Provides type-safe error handling with rich utility methods."
version: 1.2.0
license: MIT
homepage: https://github.com/cos-overclock/steady
repository: https://github.com/cos-overclock/steady

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.19.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
