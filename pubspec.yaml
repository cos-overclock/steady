name: dart_result
description: "A Result type library for Dart inspired by Rust's Result<T, E>. Provides type-safe error handling with rich utility methods."
version: 1.0.0
license: MIT
homepage: https://github.com/cos-overclock/dart_result
repository: https://github.com/cos-overclock/dart_result

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  freezed_annotation: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.10.4
  freezed: ^3.2.3